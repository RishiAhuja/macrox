import 'dart:ui';

class AppColors {
  // static const primaryLight = Color.fromARGB(255, 255, 186, 157);
  static const primaryLight = Color.fromARGB(255, 53, 204, 88);
  static const secondaryLight = Color.fromARGB(255, 2, 8, 137);
  // static const primaryDark = Color.fromARGB(255, 243, 131, 83);
  static const primaryDark = Color.fromARGB(255, 15, 130, 42);
  static const darkBackground = Color.fromARGB(255, 32, 31, 31);
  static const darkLightBackground = Color.fromARGB(255, 37, 37, 37);
  static const lightLightBackground = Color.fromARGB(255, 245, 245, 245);
  static const accentGreen = Color(0xFF8EEEA7);
  static const deepGreen = Color(0xFF005023);
  static const lightBackground = Color(0xFFF0FFF4);
  static const subtleGrey = Color(0xFFE8E8E8);
}

class NexusColors {
  // Primary colors
  static const primaryBlue =
      Color(0xFF3D5AFE); // Main brand color - signal blue
  static const primaryPurple =
      Color(0xFF6A11CB); // Secondary brand color - deep network purple
  static const signalGreen = Color(0xFF4CAF50); // Success/active connection

  // Background colors
  static const darkBackground = Color(0xFF121212); // Dark mode background
  static const darkSurface = Color(0xFF1E1E1E); // Dark mode surface/card bg
  static const lightBackground =
      Color(0xFFF7F9FC); // Light mode background - slight blue tint

  // Accent colors
  static const dataOrange = Color(0xFFFF6E40); // Data streams/activity
  static const nexusTeal = Color(0xFF00BCD4); // Highlights/accents
  static const signalGray = Color(0xFF78909C); // Inactive/disabled elements

  // Gradient components
  static const gradientStart = Color(0xFF6A11CB); // Purple start for gradients
  static const gradientEnd = Color(0xFF3D5AFE); // Blue end for gradients

  static const Color successGreen = Color(0xFF4CAF50);
  static const Color errorRed = Color(0xFFE53935);
}
